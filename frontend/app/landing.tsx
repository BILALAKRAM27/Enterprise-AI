// app/landing.tsx
// Route file for the /landing screen (public, unauthenticated users only).
// The actual screen component lives in EnterpriseAILandingScreen.tsx —
// this is just the Expo Router entry point.
export { default } from './EnterpriseAILandingScreen';
