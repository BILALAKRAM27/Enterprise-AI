// components/tokens.ts
// Re-exports from the central theme package so component files can use
// the shorter relative path `'./tokens'` without duplicating token logic.
export { ribbonForScore } from '../theme/tokens';
export type { Tokens } from '../theme/tokens';
